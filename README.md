# RF Energy Detection

## Getting Started

Clone into the repository and install the required dependences:
```
git clone https://github.com/ChrisP227/energy-detection.git
cd energy-detection
pip install -r requirements.txt
```

Generate dataset:
```
chmod +x generate_data.sh
./generate_data.sh
```